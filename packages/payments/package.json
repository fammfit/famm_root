{
  "name": "@famm/payments",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@famm/db": "*",
    "@famm/types": "*",
    "stripe": "^16.1.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@famm/config": "*",
    "@types/node": "^20.14.0",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
